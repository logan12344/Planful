<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/theme_light_background_color"/>
    <stroke android:width="0.8dp" android:color="@color/color_primary" />
    <corners android:radius="14dp" />
</shape>